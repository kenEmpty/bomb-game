/* =========================================================================
 * config.js
 * ゲーム全体のバランス調整用 定数。ここを書き換えるだけで挙動を変えられる。
 * ========================================================================= */

const CONFIG = {
  /* --- バランス調整パラメータ（要望どおり定数管理） --- */
  MOVE_STEPS: 3,          // 1ターンの移動歩数（初期値3）
  BOMB_MAX_RANGE: 2,      // 爆弾の最大射程（初期値2）
  OBSTACLE_MIN: 2,        // ランダム障害物の最小数（初期値2）
  OBSTACLE_MAX: 4,        // ランダム障害物の最大数（初期値4）
  CPU_THINK_TIME: 600,    // CPUの思考演出時間(ms)（Step2で使用）
  ANIM_SPEED: 1.0,        // アニメーション速度の倍率（Step3で使用。大きいほど速い）
  CELL_MAX_STRETCH: 1.3,  // セルの縦横比の許容上限。空き縦スペースを使ってマスを縦に広げ、タップしやすくする

  /* --- マップサイズ定義（cols=横, rows=縦） --- */
  FIELD_SIZES: {
    small:  { cols: 8,  rows: 7,  label: '小 8×7' },
    medium: { cols: 10, rows: 9,  label: '中 10×9' },
    large:  { cols: 12, rows: 10, label: '大 12×10' },
  },

  /* --- プレイヤーの色（行動順 1〜4 に対応） --- */
  PLAYER_COLORS: ['#ff5470', '#2ec4b6', '#3a86ff', '#ffb703'],

  /* --- 8方向ベクトル（dr=行方向, dc=列方向） --- */
  DIRS_8: [
    { dr: -1, dc:  0 }, // 上
    { dr:  1, dc:  0 }, // 下
    { dr:  0, dc: -1 }, // 左
    { dr:  0, dc:  1 }, // 右
    { dr: -1, dc: -1 }, // 左上
    { dr: -1, dc:  1 }, // 右上
    { dr:  1, dc: -1 }, // 左下
    { dr:  1, dc:  1 }, // 右下
  ],
};

/* セルの状態 */
const CELL = {
  NORMAL: 0,     // 通常マス
  DESTROYED: 1,  // 破壊済みマス（進入不可）
};

/* ゲームのフェーズ（ターン内の段階） */
const PHASE = {
  THROW: 'throw', // ①爆弾を投げる
  MOVE:  'move',  // ②移動する
  OVER:  'over',  // ゲーム終了
};
